# 병원경영 자동화 리포트 페이지

이 저장소는 병원경영 자동화 리포트 랜딩 페이지를 정적 파일로 제공합니다.

## 실행 방법

다음 명령으로 로컬 서버를 실행한 뒤 브라우저에서 확인할 수 있습니다.

```bash
python -m http.server 8000
```

브라우저에서 `http://localhost:8000` 에 접속하세요.

## 파일 구성

- `index.html`: 랜딩 페이지 본문
- `styles.css`: 스타일시트
